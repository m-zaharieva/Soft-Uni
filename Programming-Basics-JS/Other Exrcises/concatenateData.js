function concatenateData(first, last, age, town){
    let a = first;
    let b = last;
    let c = Number(age);
    let d = town;
    console.log(`You are ${a} ${b}, a ${c}-years old person from ${d}.`);
}

concatenateData("Miroslava", "Zaharieva", "32", "Pazardzhik");