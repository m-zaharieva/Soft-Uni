function invalidNumber (numberInput) {
    let number = Number(numberInput);

    if (number >=100 && number <= 200) {

    } else if(number === 0) {

    } else {
        console.log("invalid");
    }

}

invalidNumber(75);